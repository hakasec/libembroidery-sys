[package]
name = "libembroidery-sys"
version = "0.1.0"
authors = ["Declan Soper <soperdeclan@gmail.com>"]
edition = "2018"

build = "build.rs"

[dependencies]
libc = "0.2"

[build-dependencies]
bindgen = "0.42.2"
